# คู่มืออ่าน `app.py` ทีละส่วนตามเลขบรรทัด

ไฟล์ `app.py` มี 785 บรรทัด แบ่งเป็น 3 ส่วน: การตั้งค่าและนำเข้าโค้ด (1–35), CSS สำหรับหน้าตาแชต (36–620), และตรรกะของแชตบอท (624–785) บรรทัดว่างมีไว้จัดระเบียบจึงไม่ทำงาน ส่วนวงเล็บปิดและ `}` ปิดคำสั่งหรือบล็อกที่เปิดไว้ก่อนหน้า

## บรรทัด 1–35: เริ่มต้นแอป

| บรรทัด | คำอธิบาย |
| --- | --- |
| 1 | นำเข้า `base64` เพื่อแปลงรูปโปรไฟล์เป็นข้อความที่ใส่ใน HTML ได้ |
| 2 | นำเข้า `logging` สำหรับบันทึกข้อผิดพลาดโดยไม่แสดงข้อมูลระบบแก่ผู้ใช้ |
| 3 | นำเข้า `os` เพื่ออ่านตัวแปรแวดล้อม เช่น API key |
| 4 | นำเข้า `Path` เพื่อจัดการตำแหน่งไฟล์อย่างปลอดภัย |
| 6 | นำเข้า Streamlit และตั้งชื่อย่อว่า `st` |
| 7 | นำเข้า `load_dotenv` สำหรับอ่านไฟล์ `.env` |
| 8 | นำเข้า `time` เพื่อวัดเวลาการตอบ |
| 9 | นำเข้าฟังก์ชันเชื่อมต่อ Gemini และสร้างห้องสนทนา |
| 10–12 | นำเข้าฟังก์ชัน RAG: โหลด FAQ, ค้นหา, สร้าง prompt, สร้างคำตอบ และค่าคงที่ที่ใช้กับแชต |
| 14 | สร้างตัวบันทึกเหตุการณ์ของไฟล์นี้ |
| 16 | เริ่มตั้งค่าหน้าเว็บ Streamlit |
| 17 | ตั้งชื่อแท็บของเบราว์เซอร์ |
| 18 | ตั้งไอคอนแท็บเป็นสายฟ้า |
| 19 | จำกัดพื้นที่เนื้อหาให้อยู่กึ่งกลาง |
| 20 | ความเห็น: แถบด้านข้างจะแสดงหรือพับตามขนาดหน้าจอ |
| 21 | ใช้พฤติกรรมแถบด้านข้างแบบอัตโนมัติ |
| 22 | ปิดคำสั่งตั้งค่าหน้าเว็บ |
| 24 | หาโฟลเดอร์ที่ไฟล์ `app.py` อยู่ แล้วเก็บไว้ใน `APP_DIR` |
| 25 | ระบุที่อยู่ไฟล์รูปโปรไฟล์บอท |
| 28 | เริ่มฟังก์ชันแปลงรูปเป็น data URI |
| 29 | คำอธิบายของฟังก์ชัน |
| 30 | อ่านไบต์รูปภาพ เข้ารหัส Base64 แล้วแปลงเป็นข้อความ UTF-8 |
| 31 | เติมคำนำหน้า `data:image/png;base64,` เพื่อให้ HTML แสดงรูปได้ |
| 34 | เรียกฟังก์ชันครั้งเดียว แล้วเก็บ URI รูปโปรไฟล์ไว้ใช้ซ้ำ |

## บรรทัด 36–299: CSS ชุดแรก

บรรทัด 36 เรียก `st.markdown` เพื่อฝัง HTML, 37–38 เปิดข้อความ CSS และแท็ก `<style>`, 297 ปิดข้อความ, 298 อนุญาต HTML, 299 ปิดคำสั่ง Streamlit

| บรรทัด | คำอธิบาย |
| --- | --- |
| 39–46 | เปิด `:root` และประกาศตัวแปรสีส้ม ทอง ครีม สีข้อความ และสีข้อความรอง เพื่อใช้ซ้ำทั้งหน้าจอ |
| 48–55 | ตั้งพื้นหลังไล่เฉดของแอป, สีข้อความเริ่มต้น และรายชื่อฟอนต์ภาษาไทย |
| 57–59 | ทำพื้นหลังแถบหัวของ Streamlit โปร่งใส |
| 61–65 | จำกัดความกว้างเนื้อหา 920px และเว้นพื้นที่บน–ล่าง |
| 67–79 | กำหนดกล่อง hero: ใช้ flex, ระยะห่าง, ขอบโค้ง, พื้นหลังไล่สี และเงา |
| 81–89 | สร้างสายฟ้าตกแต่งด้วย pseudo-element `::after` แล้วกำหนดตำแหน่ง สี ขนาด และการหมุน |
| 91–101 | สร้างกรอบวงกลมของรูป avatar กำหนดขนาด พื้นหลังไล่สี และเงา |
| 103–111 | ทำให้รูปภายในกรอบเต็มพื้นที่ เป็นวงกลม มีขอบขาว และครอบรูปโดยไม่ยืด |
| 113–116 | วางข้อความใน hero ไว้เหนือสายฟ้าตกแต่งด้วย `z-index` |
| 118–125 | จัดรูปแบบข้อความหัวข้อเล็ก: สี ขนาด น้ำหนัก ระยะอักษร และตัวพิมพ์ใหญ่ |
| 127–137 | จัดหัวข้อหลักของ hero และเปลี่ยนสีข้อความที่อยู่ใน `span` |
| 139–145 | จำกัดความกว้างและจัดสี ระยะ และความสูงบรรทัดของย่อหน้า hero |
| 147–158 | จัดกล่องสถานะให้เป็นป้ายทรงแคปซูล มีช่องว่าง ขอบโค้ง พื้นหลังและเงา |
| 160–166 | สร้างจุดสีเขียวสำหรับสถานะพร้อมวงเงารอบจุด |
| 168–174 | จัดป้ายชื่อส่วนของหน้า |
| 176–183 | จัดกล่องข้อความแชต: ระยะห่าง ขอบ พื้นหลัง และเงา |
| 185–191 | ทำ avatar ในกล่องแชตให้เป็นวงกลมและมีขอบสีส้ม |
| 193–196 | กำหนดสีและระยะบรรทัดข้อความแชต |
| 198–203 | กำหนดขอบ พื้นหลัง และเงาของช่องพิมพ์ข้อความ |
| 205–208 | เปลี่ยนขอบและเงาเมื่อผู้ใช้กำลังพิมพ์ในช่องข้อความ |
| 210–212 | เปลี่ยนสีปุ่มส่งข้อความ |
| 214 | ความเห็นอธิบาย CSS ของพื้นที่ล่าง |
| 215–226 | ทำพื้นหลังด้านล่างแบบไล่ความโปร่งใส และทำกล่องย่อยโปร่งใส |
| 228–238 | ทำให้พื้นที่ช่องพิมพ์กว้างเท่าพื้นที่แชต |
| 240–245 | จัดกล่อง expander ให้มีขอบโค้งและพื้นหลังอ่อน |
| 247–253 | ตั้งพื้นหลังไล่สีของ sidebar และทำข้อความภายในเป็นสีขาว |
| 255–268 | จัดปุ่มใน sidebar และเปลี่ยนหน้าตาเมื่อวางเมาส์ |
| 270–295 | CSS สำหรับจอเล็ก: ลดระยะ, ลดขนาด avatar และปรับความกว้างช่องพิมพ์ |

## บรรทัด 301–620: CSS ชุดที่สอง

บรรทัด 301–302 เป็นความเห็นว่าชุดนี้ปรับหน้าตาการสนทนาเท่านั้น บรรทัด 303–620 มีโครงสร้างเหมือน CSS ชุดแรก: เปิด `st.markdown`, เปิด `<style>`, ปิดข้อความ และอนุญาต HTML

| บรรทัด | คำอธิบาย |
| --- | --- |
| 306–317 | ประกาศตัวแปรสีใหม่สำหรับโทนกระดาษครีมของหน้าสนทนา |
| 319–325 | ตั้งพื้นหลัง สีข้อความ และฟอนต์สำหรับหน้าจอหลัก โดยเขียนทับค่าบางส่วนจาก CSS ชุดแรก |
| 327–330 | ทำ header ของ Streamlit กึ่งโปร่งใสและเบลอพื้นหลัง |
| 332–336 | ปรับความกว้างเนื้อหาเป็น 860px และระยะบน–ล่างใหม่ |
| 338–351 | สร้าง app bar แบบติดด้านบน มี flex, ขอบล่าง, พื้นหลังโปร่งใส และ blur |
| 353–358 | จัดกลุ่มตราสินค้าใน app bar ให้วางแนวนอน |
| 360–367 | กำหนดขนาดและทรงกลมของ avatar ใน app bar |
| 369–387 | กำหนดรูปแบบชื่อและคำบรรยายใน app bar; `ellipsis` ตัดข้อความยาวด้วยจุดสามจุด |
| 389–404 | จัดสถานะ app bar และสร้างจุดนำหน้าด้วย `::before` |
| 406–414 | ยกเลิกกล่องและเงาเดิมของข้อความแชต เพื่อใช้หน้าตาแบบเรียบ |
| 416–422 | กำหนดขนาด avatar ในข้อความแชต |
| 424–436 | จำกัดความกว้างคำตอบ บังคับตัดคำยาว และกำหนดขนาดตัวอักษร |
| 438–471 | แยกหน้าตาข้อความผู้ใช้ให้เป็นฟองด้านขวา ซ่อน avatar ผู้ใช้ และกำหนดตัวอักษรในฟอง |
| 473–491 | จัดพื้นหลังและความกว้างของพื้นที่ป้อนข้อความด้านล่าง |
| 493–509 | จัดช่องพิมพ์ ปุ่มส่ง และสถานะ focus ในดีไซน์ชุดใหม่ |
| 511–521 | จัดพื้นหลัง ขอบ และสีข้อความของ sidebar ในดีไซน์ชุดใหม่ |
| 523–556 | จัดแถบแบรนด์ของ sidebar รวมถึงรูป ชื่อ และคำบรรยาย |
| 558–580 | จัดปุ่ม sidebar และ transition เมื่อวางเมาส์ |
| 582–596 | จัด expander ทั้งใน sidebar และหน้าหลัก |
| 598–616 | CSS สำหรับจอไม่เกิน 768px: ลด padding, ซ่อนคำบรรยาย/สถานะ และเพิ่มพื้นที่ฟองผู้ใช้ |

## บรรทัด 624–639: ค่าตั้งต้นและ Gemini

| บรรทัด | คำอธิบาย |
| --- | --- |
| 624 | อ่านค่าใน `.env` ที่อยู่ข้างไฟล์แอป |
| 625 | อ่าน API key จากตัวแปรแวดล้อม |
| 627 | ตรวจว่าหา API key ไม่พบหรือไม่ |
| 628–631 | ลองอ่าน API key จาก Streamlit Secrets; ถ้าไม่มีไฟล์หรือไม่มีคีย์ ให้ข้ามโดยไม่ทำให้แอปล้ม |
| 633 | บอก Streamlit ให้เก็บ resource ที่สร้างในฟังก์ชันถัดไปไว้ใช้ซ้ำ และไม่แสดง spinner |
| 634 | เริ่มฟังก์ชันสร้าง Gemini client ที่แคชได้ |
| 635 | ส่ง key ให้ `create_client` ซึ่งอยู่ใน `gemini_service.py` |
| 638 | สร้างหรือใช้ Gemini client ที่แคชไว้เมื่อมี API key; หากไม่มีตั้งเป็น `None` |
| 639 | สร้างฟังก์ชันสร้าง embedding จาก client; ถ้าไม่มี client ฟังก์ชันนี้เป็น `None` |

## บรรทัด 642–651: เริ่มแชตใหม่

| บรรทัด | คำอธิบาย |
| --- | --- |
| 642 | เริ่มฟังก์ชันล้างประวัติ |
| 643–648 | แทนประวัติด้วยข้อความทักทายของบอทหนึ่งข้อความ |
| 649 | ลบบริบท FAQ ของคำถามก่อนหน้า |
| 650 | ลบเวลาพักการเรียก embedding หลัง API ขัดข้อง |
| 651 | สั่ง Streamlit แสดงหน้าใหม่ทันที |

## บรรทัด 654–685: Sidebar และ app bar

| บรรทัด | คำอธิบาย |
| --- | --- |
| 654 | เปิดบล็อกที่ทุกคำสั่งภายในจะแสดงใน sidebar |
| 655–666 | แสดง HTML ของตรา PELEK และรูป avatar ที่แปลงเป็น data URI |
| 667 | สร้างปุ่ม “แชตใหม่”; ถ้าผู้ใช้กดจะเป็นจริง |
| 668 | เรียก `clear_history()` เมื่อกดปุ่ม |
| 669 | แสดงคำอธิบายสั้นใน sidebar |
| 671–685 | แสดง app bar ด้านบนของหน้าหลัก พร้อมรูป ชื่อ คำบรรยาย และสถานะพร้อมตอบ |

## บรรทัด 687–722: เริ่มสถานะและโหลด FAQ

| บรรทัด | คำอธิบาย |
| --- | --- |
| 687 | ตรวจว่าการสนทนารอบนี้มี `messages` แล้วหรือยัง |
| 688–693 | ถ้ายังไม่มี ให้สร้างข้อความทักทายเริ่มต้น |
| 695 | ระบุตำแหน่งไฟล์ FAQ หลักไฟล์เดียว |
| 698 | เริ่มตัวตกแต่ง cache สำหรับ retriever |
| 699 | กำหนดฟังก์ชันที่รับเนื้อหา FAQ เวอร์ชัน embeddings และเวอร์ชันคลังคำศัพท์ |
| 700 | สร้าง retriever จาก FAQ และโฟลเดอร์แอป |
| 703 | เริ่มดักข้อผิดพลาดจากการอ่าน FAQ หรือ embeddings |
| 704–707 | สร้าง tuple เวอร์ชันจากเวลาแก้ไขและขนาดไฟล์ embeddings เพื่อให้ Streamlit ล้าง cache เมื่อไฟล์เปลี่ยน |
| 708–710 | อ่าน FAQ, embeddings, synonyms และ question aliases แล้วส่งเข้า cache function |
| 711 | รับข้อผิดพลาดจากไฟล์หาย, ข้อมูลไม่ถูกต้อง หรือการถอดรหัสข้อความล้มเหลว |
| 712 | แสดงข้อความผิดพลาดที่ผู้ใช้เข้าใจได้ |
| 713 | หยุดแอป เพราะไม่มีข้อมูลที่เชื่อถือได้ให้ตอบ |
| 715 | เปิด sidebar อีกครั้ง |
| 716 | สร้างกล่องข้อมูลสำหรับผู้ดูแล |
| 717 | ตรวจว่ามีคำเตือนเรื่อง embeddings หรือไม่ |
| 718 | แสดงคำเตือนของ retriever |
| 719–720 | ถ้าไม่มีคำเตือน แสดงว่า embeddings ตรงกับ FAQ |
| 721 | ตรวจ API key อีกรอบเพื่อบอกสถานะความสามารถของบอท |
| 722 | แจ้งว่าหากไม่มี API key ยังตอบ FAQ ที่ตรงได้ |

## บรรทัด 724–728: แสดงประวัติ

| บรรทัด | คำอธิบาย |
| --- | --- |
| 724 | วนอ่านข้อความทั้งหมดในประวัติ |
| 725 | ตรวจว่าข้อความนั้นเป็นของบอทหรือไม่ |
| 726 | แสดงข้อความบอทพร้อม avatar |
| 727 | กรณีไม่ใช่ข้อความบอท |
| 728 | แสดงเป็นข้อความของผู้ใช้ |

## บรรทัด 730–785: รับคำถามและสร้างคำตอบ

| บรรทัด | คำอธิบาย |
| --- | --- |
| 730 | แสดงช่องพิมพ์; ถ้าผู้ใช้ส่งข้อความ ให้เก็บไว้ใน `prompt` แล้วเข้าบล็อกนี้ |
| 731 | ทำความสะอาดช่องว่าง อักขระแฝง และรูปแบบข้อความ |
| 732–734 | ถ้าหลังทำความสะอาดไม่มีข้อความ ให้เตือนและหยุดรอบนั้น |
| 735 | บันทึกเวลาต้นทางเพื่อคำนวณเวลาตอบรวม |
| 736 | แปลงประวัติเดิมเป็นรูปแบบที่ Gemini ใช้ |
| 737 | เพิ่มคำถามของผู้ใช้เข้า session state |
| 738 | แสดงคำถามทันทีบนหน้าเว็บ |
| 739 | กำหนด `result` เป็น `None` เผื่อการค้นหาล้มเหลว |
| 740 | เปิดกล่องข้อความคำตอบของบอท |
| 741 | สร้าง placeholder เพื่อเปลี่ยนข้อความระหว่าง streaming ได้ |
| 742 | ตั้งธงว่าคำตอบอ้างอิง FAQ ได้จริงหรือไม่ |
| 743 | เริ่มดักข้อผิดพลาดของกระบวนการตอบ |
| 744 | ค้นหา FAQ และตัดสินใจเส้นทางตอบ: direct, local หรือ semantic |
| 745 | อ่านคำตอบแบบ direct ถ้ามี |
| 746 | ถ้าไม่มีคำตอบ direct ต้องเรียบเรียงจาก FAQ |
| 747 | ตรวจว่ามี API key สำหรับ Gemini หรือไม่ |
| 748 | แจ้งผู้ใช้เมื่อไม่สามารถเรียบเรียงคำตอบได้เพราะไม่มี API key |
| 749 | กรณีมี API key |
| 750 | เริ่มจับเวลาส่วน Gemini |
| 751 | เริ่มบล็อกที่รับประกันว่าจะบันทึกเวลาแม้เรียก Gemini ล้มเหลว |
| 752 | สร้างห้องสนทนา Gemini จาก client และประวัติก่อนหน้า |
| 753–754 | เรียก Gemini แบบ streaming โดยส่ง prompt ที่มี FAQ ที่ค้นพบ และใช้ placeholder แสดงผลระหว่างตอบ |
| 755 | เก็บสถานะว่า SDK stream ได้หรือไม่ |
| 756 | ตั้งธง `grounded_answer` ถ้าคำตอบไม่ใช่ข้อความ “ไม่พบข้อมูล” |
| 757 | เริ่ม `finally` ซึ่งทำงานเสมอ |
| 758 | บันทึกเวลาที่ Gemini ใช้ |
| 759–760 | กรณีมี direct FAQ ตั้งธงว่าเป็นคำตอบอ้างอิงข้อมูล |
| 761 | แสดงคำตอบสุดท้ายแทน placeholder |
| 762 | ตรวจว่าคำตอบใช้ข้อมูล FAQ ได้จริง |
| 763 | บันทึกบริบท FAQ ไว้ใช้กับคำถามต่อเนื่อง |
| 764 | รับข้อผิดพลาดใด ๆ ในบล็อกตอบคำถาม |
| 765 | ความเห็น: ห้ามแสดงข้อความบางส่วนหรือข้อมูล SDK ต่อผู้ใช้ |
| 766 | บันทึก stack trace ไว้ใน log สำหรับผู้ดูแล |
| 767 | สร้างข้อความผิดพลาดที่ปลอดภัยสำหรับผู้ใช้ |
| 768 | แสดงข้อความผิดพลาดแทน placeholder |
| 769 | บันทึกคำตอบสุดท้าย ไม่ว่าจะสำเร็จหรือล้มเหลว ลงประวัติ |
| 770 | แสดงรายละเอียดเฉพาะเมื่อมีผลค้นหา |
| 771 | คำนวณเวลาตอบรวม |
| 772 | เปิด expander สำหรับตรวจสอบ FAQ และเวลา |
| 773 | แสดงเส้นทางค้นหาและวิธีที่ใช้ |
| 774 | ตรวจว่ามีการใช้บริบทคำถามก่อนหน้าหรือไม่ |
| 775–778 | แสดงหัวข้อเดิมและคำค้นที่รวมบริบทแล้ว |
| 779 | ตรวจว่า SDK ไม่รองรับ streaming หรือไม่ |
| 780 | แจ้งผู้ดูแลว่าจะแสดงคำตอบเมื่อเสร็จแทนการทยอยแสดง |
| 781 | แสดงเวลาที่ใช้เป็น JSON |
| 782 | วน FAQ ที่ค้นพบตามอันดับ |
| 783 | แสดงอันดับและคะแนนค้นหา |
| 784 | แสดงเนื้อหา FAQ ที่ใช้ |
| 785 | วาดเส้นคั่นระหว่าง FAQ แต่ละรายการ |

## ลำดับการทำงานเมื่อผู้ใช้ส่งคำถาม

1. รับคำถามและทำความสะอาดข้อความที่บรรทัด 730–734
2. ค้น FAQ จากหน่วยความจำที่โหลดไว้ที่บรรทัด 703–710
3. หากคำถามตรง FAQ จะตอบทันที; หากไม่ตรงจะใช้ Gemini เรียบเรียงจาก FAQ ที่ค้นพบ
4. เก็บประวัติและบริบทเพื่อให้ผู้ใช้ถามต่อได้
5. แสดงข้อมูลวินิจฉัยใน expander สำหรับผู้ดูแล
